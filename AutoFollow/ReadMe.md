在这个项目中存入数据的同时有其他线程在访问和修改数据
比如存入从UWB读取的数据同时有线程在读取之前的数据


在Python基本数据类型中
list、tuple、dict本身就是属于线程 安全的，
所以如果有多个线程对这3种容器做操作时，
我们不必考虑线程安全问题

在这个项目中我们有关线程共享资源都是列表容器，
所以很幸运地避开了线程锁的问题
MinDistance:95cm